name: Auto Update Toolforge

on:
  push:
    branches:
      - main
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Test workspace
        run: |
          echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE" # /home/runner/work/svg_translate/svg_translate # ls -al

      - name: Copy files to server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: 22
          source: "./"
          target: "~/web_make2_temp"
          rm: true

      - name: Deploy on Toolforge
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          port: 22
          request_pty: true
          script_stop: true
          key: ${{ secrets.KEY }}
          script: |
            become armake bash -lc "
              rm -rf ~/make2-web &&
              cp -r /mnt/nfs/labstore-secondary-tools-home/${{ secrets.USERNAME }}/web_make2_temp ~/make2-web &&
              chmod -R 770 ~/make2-web &&
              cp -rf ~/make2-web/web_sh/*.sh ~/web_sh -v &&
              ~/web_sh/install_make2.sh ${{ secrets.GF_TOKEN }} &&
              ~/web_sh/install_ma_lists.sh ${{ secrets.GF_TOKEN }} &&
              ~/web_sh/update1.sh &&
              rm -rf ~/make2-web &&
              echo \"Updated on: $(date '+%Y-%m-%d %H:%M:%S')\" > ~/update_from_github &&
              toolforge-jobs run pipup --image python3.11 --command "~/web_sh/pip.sh" --wait &&
              toolforge-webservice python3.11 stop &&
              toolforge-webservice python3.11 start
            "
            rm -rf web_make2_temp
